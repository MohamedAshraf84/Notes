<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/sort_by_title"
        android:icon="@drawable/ic_sort_by_title"
        android:title="@string/sort_by_title"
        app:showAsAction="ifRoom|withText"/>

    <item android:id="@+id/sort_by_creation_date"
        android:icon="@drawable/ic_date"
        android:title="@string/sort_by_creation_date"
        app:showAsAction="ifRoom|withText"/>

    <item android:id="@+id/sort_by_modification_date"
        android:icon="@drawable/ic_modify"
        android:title="@string/sort_by_modification_date"
        app:showAsAction="ifRoom|withText"/>

</menu>