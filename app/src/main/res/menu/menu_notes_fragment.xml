<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.mohamedashraf.notes.ui.activities.MainActivity">
    <item
        android:id="@+id/action_sort"
        android:icon="@drawable/ic_sort"
        android:title="@string/sort"
        app:showAsAction="always">
       <!-- <menu >
            <item android:id="@+id/sort"
                android:icon="@drawable/ic_sort_by_title"
                android:title="@string/app_name"
                app:showAsAction="ifRoom|withText"/>

            <item android:icon="@drawable/ic_sort_by_time"
                android:title="@string/app_name"
                app:showAsAction="ifRoom|withText"/>

            <item android:icon="@drawable/ic_finish"
                android:title="@string/app_name"
                app:showAsAction="ifRoom|withText"/>
        </menu>-->
    </item>

    <item
        android:id="@+id/action_layout"
        android:icon="@drawable/ic_layout"
        app:showAsAction="ifRoom"
        android:title="@string/change_layout">
        <!--<menu >
            <item
                android:id="@+id/layout1"
                android:icon="@drawable/ic_time"
                android:title="@string/app_name"
                app:showAsAction="ifRoom|withText"
                />

            <item
                android:id="@+id/layout2"
                android:icon="@drawable/ic_time"
                android:title="@string/app_name"
                app:showAsAction="ifRoom|withText"
                />
        </menu>-->
    </item>

</menu>